{
    "extends": "../.eslintrc.json",
    "ignorePatterns": ["build/*", "dev-build/*"],
    "rules": {
        "@typescript-eslint/no-unused-vars": ["warn",
            {
                "varsIgnorePattern": "^_.*"
            }
        ]
    }
}